---
driver:
  name: terraform

provisioner:
  name: terraform

platforms:
  - name: ubuntu
 
verifier:
  name: terraform
  systems:
    - name: default
      backend: local
      controls:
      - check_string

suites:
  - name: default
